# weather-normals

## プロジェクトの目的
Open-Meteo の ERA5 アーカイブを使い、今日の気温が平年と比べて
どうか、過去何位かを表示するアプリ。
開発者は初心者であり、コードを読んで理解することが目的に含まれる。

## 開発方針
- 一度に大量のコードを書かない。1ステップずつ、動作確認しながら進める
- 新しい概念やライブラリを使うときは、なぜそれを選んだかをコメントか
  会話で説明する
- 依存ライブラリは最小限にする。標準ライブラリで足りるなら使わない
- 説明は日本語で行う

## 技術スタック
- Python 3.10 / 仮想環境は .venv
- Step 1 は単体スクリプトのみ。FastAPI や React はまだ導入しない

## データ仕様
- 取得元: https://archive-api.open-meteo.com/v1/archive
- ERA5 は 0.25度グリッド。指定座標に最も近い格子点の値が返る
- ERA5 は数日の遅延あり。当日値は Forecast API が必要（Step 1 では扱わない）
- 30年分は1リクエストで一括取得する。日ごとに叩かない
