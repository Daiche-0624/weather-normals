# weather-normals

## プロジェクトの目的
Open-Meteo の ERA5 アーカイブを使い、今日の気温が平年と比べて
どうか、過去何位かを表示するアプリ。
開発者は初心者であり、コードを読んで理解することが目的に含まれる。

## 開発方針
- 一度に大量のコードを書かない。1ステップずつ、動作確認しながら進める
- 新しい概念やライブラリを使うときは、なぜそれを選んだかをコメントか
  会話で説明する
- 依存ライブラリは最小限にする。標準ライブラリで足りるなら使わない
- 説明は日本語で行う

## 進捗
- Step 1(単体スクリプト `fetch_era5.py`)完了
- Step 2(FastAPI化)完了。取得・計算ロジックを `core.py` に切り出し
  (`build_report()` が dict を返す、print なし)、`cli.py`(argparse+print)
  と `main.py`(FastAPI、`GET /api/report`)から共通で呼び出す構成にした。
  詳細は README.md 参照
- 既知の不具合として、気候セクション(10年ごとの平均)の月・年の
  決め打ち(8月固定、2026年除外)がある。FastAPI化と切り分けるため
  今回は直さず、別ブランチで修正する予定
- 次のステップは未定(React等フロントエンドの導入時期も未定)

## 技術スタック
- Python 3.10 / 仮想環境は .venv
- Step 1 は単体スクリプトとして完了。Step 2 で FastAPI(+ uvicorn)を導入した
  (`requirements.txt` 参照。CLI単体では引き続き標準ライブラリのみで動作する)
  (React 等フロントエンドの導入時期は未定)

## データ仕様
- 過去データ取得元: https://archive-api.open-meteo.com/v1/archive
  (ERA5、0.25度グリッドで指定座標に最も近い格子点の値が返る。数日の遅延あり)
- 当日値取得元: https://api.open-meteo.com/v1/forecast
  (ERA5とは異なるモデル・グリッドのため、直近7日間の重なりから
  バイアス補正して使っている。詳細はREADME.md参照)
- 過去データは30年〜85年分を1リクエストで一括取得する。日ごとに叩かない
- ERA5の古い年代(特に1940年代前後)は、地点によってデータ品質に
  既知の問題がある可能性がある。詳細はREADME.mdの「既知の制約」参照

## 平年値の定義
- 平年値A: 1991-2020年の固定30年間(気象庁の公式基準と同じ期間)
- 平年値B: 直近30年のローリング
- どちらも気象庁と同じKZフィルタ(9日移動平均を3回、逐次適用)で
  平滑化している(`--raw` オプションで平滑化前の生データに切り替え可能)
- AとBの差そのものが、その地点の温暖化の目安になる
